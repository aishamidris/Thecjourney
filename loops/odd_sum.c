#include <stdio.h>

int main(void)
{                                                   int n, odd, sum, k;                                                                            printf("Input the number of terms: ");
	scanf("%d", &n);
                                                    for (k = 1; k <= n; k++)
						    if (k % 2 == 0){
						    printf("the odd numbers are: %d", odd);}
						    else {printf(" ");}
						    {					    sum += n;
						    printf("The sum is: %d\n", sum);
						    }
        return 0;
}
